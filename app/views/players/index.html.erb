<table>
  <% @players.each do |player| %>
    <tr class="player">
      <td><%= player.name %></td>
      <td><%= player.job %></td>
      <td><%= player.health %></td>
      <td><%= player.magic %></td>
    </tr>
  <% end %>
</table>
